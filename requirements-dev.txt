# This file defines the additional requirements for a developer working
# with this project (e.g. for testing or useful development tools).
# The regular project dependencies are defined in requirements.txt

# testing
pytest>=9.0.3          # 9.0.3 fixes PYSEC-2026-1845
pytest-cov>=5.0
responses~=0.25

# linting & security (also run via pre-commit)
ruff~=0.8
bandit~=1.8
pip-audit~=2.7

# documentation
Sphinx~=8.1
sphinx-rtd-theme~=3.0
myst-parser~=4.0

# publishing
wheel
twine

# git hooks
pre-commit
